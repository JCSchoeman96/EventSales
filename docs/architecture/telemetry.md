# Telemetry Plan

Track webhook latency, accepted/rejected counts, DB insert latency, Oban queue health, REST latency/errors/429s, missing mapping recovery, cache hit/miss, HotStateAggregator rebuilds, and CSV import durations.
